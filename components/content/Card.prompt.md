**Card** — base white surface with soft navy shadow. Wrap any content.

```jsx
<Card>…</Card>
<Card interactive padding="0">…clickable…</Card>
```

`interactive` adds hover-lift. Override `padding` (e.g. `"0"` when the child manages its own).
