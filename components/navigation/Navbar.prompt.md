**Navbar** — TCS site header (logo, nav links, phone, CTA).

```jsx
<Navbar active="Properties" />
<Navbar variant="transparent" cta="Find a Home" />
```

`variant="transparent"` sits over a hero image (light text); `solid` is white with a hairline border. `active` highlights a link in salmon.
